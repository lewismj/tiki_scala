package tiki


package object instances {

  object all extends AllInstances
  object digraph extends DigraphInstances
  object disjointSet extends DisjointSetInstances
  object edge extends EdgeInstances
  object labelledEdge extends LabelledEdgeInstances
  object point extends PointInstances
  object real extends RealInstances
  object weightedDigraph extends WeightedDigraphInstances
  object weightedEdge extends WeightedEdgeInstances
  object weightedUndirectedGraph extends WeightedUndirectedInstances
  object texInstances extends TexInstances

}
